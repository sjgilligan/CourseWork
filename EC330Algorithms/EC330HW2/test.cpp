#include <iostream>
#include <vector>
#include <bits/stdc++.h>

using namespace std;

std::vector<int> findStudents(std::vector<int> id) {
    vector<int> result = {id[0],id[1]};

    return result;
}